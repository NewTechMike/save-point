/** function hideWant(id){
      setShowStart(true)
      setShowReplay(true)
      setShowWant(false);
      {showWant ? console.log("1",showWant) : console.log("false", id)}
      checkRender();
    }  */

    /**{errors.length > 0 && (
        <ul style={{color: "red"}}>  
            <li key={errors}>{errors}</li>
        </ul>
      )} */